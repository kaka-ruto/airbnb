<%= form_for([@room, @room.reviews.new]) do |f|  %>
	
	<%= f.text_field :star %>
	<%= f.text_field :comment %>

	<%= f.hidden_field :room_id, value: @room.id %>

	<div class="actions">
		<%= f.submit "Create" %>
	</div>
<% end %>